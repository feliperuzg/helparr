import RenameScreen from '@/components/rename/RenameScreen';

export const metadata = {
  title: 'Rename — helparr',
  description: 'Preview every rename before a single file moves.',
};

export default function RenamePage() {
  return <RenameScreen />;
}
